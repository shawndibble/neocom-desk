# UI consistency ledger

Reference for the next `/ui-consistency` run: what has been swept, what is
settled, and what was filed or killed. Curated in place. Never a run log.

## Axes swept

| Axis / entity | Concluded |
| ------------- | --------- |
| _none yet_    |           |

## Settled icon map

Glyph → its one meaning, confirmed consistent app-wide. A new use of a listed
glyph for a different meaning is a finding.

| Glyph      | Meaning |
| ---------- | ------- |
| _none yet_ |         |

## Contract already enforced

- Direct `radix-ui` and `@phosphor-icons/react` imports outside
  `src/components/ui/` fail lint (`eslint.config.js`).
- Control heights come from `controlStyles.ts` for every primitive that takes
  `size` (DESIGN.md §3).

## Justified exceptions

Matrix holes and deviations confirmed deliberate, so no run re-flags them.

- _none yet_

## Standing kill-tests

See RUBRIC.md "Standing kill-tests". Add here only heuristics learned from a
run that are not yet worth promoting there.

## Filed findings

| Issue      | Verdict | Finding |
| ---------- | ------- | ------- |
| _none yet_ |         |         |

## Killed findings

- _none yet_
